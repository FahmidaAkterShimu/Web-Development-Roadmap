const isLeader = true;

// if(isLeader === true) or
if(isLeader){
    console.log('Give special treatment');
}
else{
    console('Not allow')
}

const isPassed = false;

// if(isPassed === false) or
if(!isPassed){
    console.log('Cannot get into the next class.')
}
else{
    console.log('You will get a treat')
}
